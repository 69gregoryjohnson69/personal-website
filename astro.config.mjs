// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://69gregoryjohnson69.github.io',
  base: '/personal-website',
  build: {
    inlineStylesheets: 'auto',
  },
});
